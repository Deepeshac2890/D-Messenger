<nav class="navbar navbar-expand-lg">
  <%= link_to "D-Messenger", root_path, class: "navbar-brand", id: "logo"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%# <li class="nav-item active"> %>
        <%# <%= link_to "Messages", users_path, class: "nav-link" %> 
      <%# </li>  %>
      <% if logged_in? %>
      <li class="nav-item active">
        <%= link_to "Group Chat", groupChat_path, class: "nav-link" %>
      </li>
      <% end %>
    </ul>
    <% if logged_in? %>
      <div id = "profile-dropdown" class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <div class="profile-pic">
                <img src=<%= current_user.profile_image != nil ? current_user.profile_image : "https://source.unsplash.com/250x250?girl" %> alt="Profile Picture">
          </div>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%=link_to "View Profile", user_path(current_user), class: "dropdown-item"%>
          <%=link_to "Edit Profile", edit_user_path(current_user), class: "dropdown-item"%>
          <%=link_to "Logout", logout_path, class: "dropdown-item", method: :delete%>
        </div>
      </div>
    <% else %>
      <div class="nav-item">
          <%=link_to "Login", login_path, class: "btn btn-outline-success my-2 my-sm-0"%>
      </div>
      <div class="nav-item ml-2">
          <%=link_to "Sign Up", signup_path, class: "btn btn-outline-success my-2 my-sm-0"%>
      </div>
    <% end %>
  </div>
</nav>